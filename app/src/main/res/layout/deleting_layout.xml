<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="5dp">
    <ProgressBar
        android:id="@+id/progressBarLoadMoreMsg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal" />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:gravity="center_vertical"
        android:text="Đang xóa..."
        android:textSize="20dp"
        android:layout_alignBottom="@+id/progressBarLoadMoreMsg"
        android:layout_toRightOf="@+id/progressBarLoadMoreMsg"
        android:layout_toEndOf="@+id/progressBarLoadMoreMsg"
        android:layout_marginLeft="10dp"/>
</RelativeLayout>